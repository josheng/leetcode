/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
var removeElement = (nums, val) => {
  // nums is an array
  // val is the element to be removed in place
  // if j is not val, set i = j
  // i and j++
  // if j is val, j++
  // set i = j
  let i = 0;
  for (let j = i; j <= nums.length; j++){

  }
};

nums = [0,2,1,2,4,3,3,4,0,2]
val = 2
//         i
// nums = [0,2,1,2,4,3,3,4,0,2]
//         j
// should return
// nums = [0,1,4,3,3,4,0,-,-,-]
// k = 7
